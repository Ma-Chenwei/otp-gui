# otp-gui
Software for 2FA verification that parses `otpauth://totp/` URIs.


# How to use it:
1. First, run `go mod tidy`.
2. Wait a moment.
3. Then, run `go build otp.go`.
4. The resulting executable file is the main program.
